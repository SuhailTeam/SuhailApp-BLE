{
  "name": "suhail",
  "version": "1.0.0",
  "description": "Suhail — AI relay server for the BLE smart-glasses app (visually impaired users)",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-rekognition": "^3.887.0",
    "@types/multer": "^2.1.0",
    "express": "^4.22.2",
    "multer": "^2.1.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.6",
    "@types/sharp": "^0.32.0",
    "typescript": "^5.3.0"
  }
}
